name: ai-dev
root: .
windows:
  - dev:
      layout: tiled
      panes:
        - make run
        - make test
        - make aider-refactor
        - echo "Monitor: use 'gh pr status --watch' after pushing"
